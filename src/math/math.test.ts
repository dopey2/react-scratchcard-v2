import { describe, it } from 'vitest';

describe('distanceBetween', () => {
  it.todo('returns correct distance for known points');
  it.todo('returns 0 when both points are the same');
  it.todo('returns 0 when either point is null');
});

describe('angleBetween', () => {
  it.todo('returns correct angle for known points');
  it.todo('returns 0 when either point is null');
});
